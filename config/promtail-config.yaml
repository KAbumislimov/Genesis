# Promtail для этой машины. Меняйте host: если нужно.
server:
  http_listen_port: 9080
  grpc_listen_port: 0
positions:
  filename: /tmp/positions.yaml
clients:
  - url: http://loki:3100/loki/api/v1/push
scrape_configs:
  - job_name: system
    static_configs:
      - targets: [localhost]
        labels:
          job: varlogs
          host: campus-server
          __path__: /var/log/*.log
  - job_name: messages
    static_configs:
      - targets: [localhost]
        labels:
          job: messages
          host: campus-server
          __path__: /var/log/messages
  - job_name: secure
    static_configs:
      - targets: [localhost]
        labels:
          job: secure
          host: campus-server
          __path__: /var/log/secure
  # client1 и client2 шлют логи сами через Promtail (push в Loki)
  # Контейнеры tg-campus-bot / tg-campus-client2 (Telegram) — отдельный job
  - job_name: docker_campus_bots
    docker_sd_configs:
      - host: unix:///var/run/docker.sock
        refresh_interval: 15s
    relabel_configs:
      - action: keep
        source_labels: [__meta_docker_container_name]
        regex: .*tg-campus-(bot|client2).*
      - action: replace
        source_labels: [__meta_docker_container_name]
        regex: .*
        replacement: campus-server
        target_label: host
      - action: replace
        source_labels: [__meta_docker_container_name]
        regex: .*
        replacement: campus_telegram_bot
        target_label: job
      - action: replace
        source_labels: [__meta_docker_container_name]
        regex: /?(.*)
        replacement: ${1}
        target_label: container
    pipeline_stages:
      - docker: {}
  - job_name: campus_bot_server
    static_configs:
      - targets: [localhost]
        labels:
          job: campus_telegram_bot
          host: campus-server
          __path__: /opt/tg-campus-bot/action.log
  
  - job_name: campus_media_server
    static_configs:
      - targets: [localhost]
        labels:
          job: campus_media
          host: campus-server
          __path__: /home/kamran/projects/campus-infra/cron-media.log

  - job_name: campus_backup
    static_configs:
      - targets: [localhost]
        labels:
          job: backup
          host: campus-server
          __path__: /var/log/campus-backup.log
