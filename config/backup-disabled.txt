# Машины, которые физически отключены / выведены из работы.
# Недельный бэкап (campus-backup.sh) их пропускает, страница «Бэкапы» показывает «Отключена» и не считает это проблемой.
# По одному ключу на строку: client1, client2, ... Чтобы вернуть машину в бэкап — удалите строку.
client2    # Client2 — выключена физически 2026-09-20
