# Журнал — 2026-09-19 (суббота)

> Ведётся автоматически: `scripts/update_journal.py` и `scripts/export_state.py` (каждые ~45 секунд при изменениях, запись идёт в GitHub вместе с кодом). Ручные пояснения — в разделе «Заметки».

## Изменения кода и конфигов

## Изменения состояния системы (права, настройки, машины, снимки БД)

## Заметки

- Устойчивость к перезагрузке и отключению питания: сторожевой watchdog на wctk (служба под пользователем, XDG_RUNTIME_DIR), проверено после перезапуска.
- «Лог активности»: строка раскрывается — кто, IP, устройство, полные детали.
- Гимн по понедельникам: на всех клиентах в crontab оставлен только гимн (08:00, внутренняя проверка «только понедельник»), без перемен и утра; работает локально без входа и интернета; звучит на том кампусе, откуда запущен.
- Кнопки «Утро»/«Перемены» — реальные переключатели групп (POST /api/schedule/toggle-group); виджеты «до звонка/следующее событие» исправлены.
- Крупные плашки ОНЛАЙН/ОФФЛАЙН на карточках кампусов; личный цвет панели; кнопка синхронизации времени на странице плеера.
- Grafana: доступ, панели для bstk и wctk; NTP на client1; анализатор звука на client1/bstk.
- «Стоп» (веб и Telegram) останавливает всё на всех кампусах безопасно (только воспроизведение, без лишних kill).
- Клиент bstk (Баил) поднят с нуля: PulseAudio, socat/jq, tmpfiles для /run/campus-player, campus-playerctl, cron.
- Кнопка National Music Day, повтор трека, быстрый стоп; громкость: максимум 160, wctk — PCM 39%/Master 90% (иначе фон).
- GitHub-бэкап восстановлен: .gitignore для ssl.key, вычищены файлы >100 МБ из истории (git filter-repo), автосинхронизация каждые 45 с.
- Дизайн: первый вариант на Tabler (/v2) — отклонён; начата разработка «Студии».
